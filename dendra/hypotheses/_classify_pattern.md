# Pre-registered hypothesis —  Classify Pattern

Pre-registered: **2026-05-01**.
Site: `src/dendra/analyzer.py:_classify_pattern`.
Fingerprint: (not yet computed).

*This file is part of the audit chain for the wrapped switch. Edits change its content hash; the hash is recorded in every subsequent gate evaluation. Edit deliberately; commit before evidence accumulates.*

## 1. Unit of decision

This single classification call site, identified by the fingerprint above. The site fingerprint is a blake2b digest over the function's normalized AST (with identifiers and literals stripped); refactors that don't change shape preserve the fingerprint, so this hypothesis survives variable renames and reformatting.

## 2. Gate criterion

`McNemarGate` with α = **0.01** and minimum 30 paired-correctness samples. The gate evaluates at every checkpoint (default: every 50 outcomes). First-clear is treated as the graduation event; subsequent checkpoints continue to validate the decision.

## 3. Expected n at graduation

**200–400 outcomes** (90% CI). Source: Default for regime=narrow (no cohort data yet — install will pull when available).

Site-shape inputs to the prediction:
- Pattern: `P1`
- Label cardinality: 7
- Analyzer priority score: 3.50
- Regime: narrow

## 4. Expected effect size

At graduation, ML accuracy must exceed Rule accuracy by **≥ 5.0 percentage points**. This is the lower bound below which we don't consider the graduation worth the operational complexity, regardless of statistical significance. Edit if your domain has a different floor.

## 5. Truth source

**Primary truth source:** *(to be filled in)*. Suggestions:

- Synchronous callable (Python function returning correct/incorrect)
- LLM judge with self-judgment-bias guardrail
- Human reviewer queue (best for safety-critical)
- Webhook to a downstream system that has ground truth

**Secondary (cross-validation):** *(optional; recommended for high-stakes sites)*. Use a different source than primary so agreement is informative.

**Tie-breaker:** *(optional)*. The source consulted when primary and secondary disagree.

## 6. Rollback rule

Drift detector fires when the wrapped function's AST hash no longer matches the hash recorded at graduation. Circuit breaker auto-rolls back to MODEL_PRIMARY when ML accuracy falls ≥ 10 pp below the rule baseline sustained over 20 verdicts. Both behaviors are default; override per the `SwitchConfig` documentation.

## Verdict (filled in by `dendra report`)

*Once the gate fires (or the timeout at outcome 1200 triggers a 'did not graduate within budget' outcome), `dendra report` populates this section with the observed-vs-predicted comparison. Until then it reads `(in flight)` for each row.*

| Predicted | Observed | Verdict |
|---|---|---|
| Graduation depth: 200–400 outcomes | (in flight) | (in flight) |
| Effect size: ≥ 5.0 pp | (in flight) | (in flight) |
| p < 0.01 at first clear | (in flight) | (in flight) |
| Drift handling: auto-rollback on AST mismatch | (no events yet) | (in flight) |

---

*Methodology: [Test-Driven Product Development](../../methodology/test-driven-product-development.md). Auto-generated by `dendra init`; review and commit before evidence accumulates. Subsequent edits change the content hash; the hash is recorded in the audit chain at every gate evaluation.*
